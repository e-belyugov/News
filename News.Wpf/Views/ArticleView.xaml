<views:MvxWpfView x:Class="News.Wpf.Views.ArticleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:News.Wpf.Views"
             xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Margin="20,20,20,0" FontWeight="Bold" TextWrapping="Wrap" FontSize="20" Text="{Binding SelectedArticle.Title}" />
                <TextBlock Margin="20" TextWrapping="Wrap" Text="{Binding SelectedArticle.Text}" />
                <Button x:Name="BackButton" Click="BackButton_Click" Height="40" Width="100">Назад</Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</views:MvxWpfView>
